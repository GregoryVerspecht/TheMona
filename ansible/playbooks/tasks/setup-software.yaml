- name: Update and upgrade the system
  apt:
    update_cache: yes
    upgrade: dist

- name: Install required packages
  apt:
    name:
      - hostapd
      - dnsmasq
      - python3
      - python3-pip
    state: present
